# Production Environment Variables
# This file is used for production builds and deployments

# API Configuration
NEXT_PUBLIC_API_URL=https://hrone-69dz.onrender.com

# Authentication Configuration (Production)
# Longer sessions for better UX, more frequent token checks for security
NEXT_PUBLIC_COOKIE_EXPIRES_DAYS=30
NEXT_PUBLIC_TOKEN_CHECK_INTERVAL_MINUTES=2

# Production Settings
NODE_ENV=production

# Add other production environment variables as needed
